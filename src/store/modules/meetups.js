import * as types from '../mutation-types';

const state = [
  all: []
];

const getters = {
  allMeetups: state => state.all
};

const actions = {
  getAllMeetups({ commit }) {
    commit(types.)
  }
};

export default {
  state,
  getters,
  actions,
  mutations
};
